.next
out
build
coverage
node_modules
public
next-env.d.ts
package-lock.json
tsconfig.tsbuildinfo
.lighthouseci

# Generated by backend/eval/publish.py and committed by the Retrieval eval job; see
# docs/adr/0001-generated-eval-results.md. Prettier collapses short arrays that
# json.dumps writes one element per line, so a formatting gate on this file fails on
# every published run and cannot be fixed by editing the file — only by teaching the
# generator Prettier's heuristics. A generated artefact is not worth that coupling.
data/evalResults.json
