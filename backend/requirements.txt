fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.13.5
pydantic-settings>=2.0.0
groq>=1.7.0
rank-bm25>=0.2.2
snowballstemmer>=3.1.1
numpy>=1.26.0
slowapi>=0.1.9
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Eval-only. These are the `dev` dependency group in pyproject.toml: the index builder
# and eval/run_eval.py use them, the served API does not, and the Dockerfile's
# `uv sync --no-dev` leaves them out of the production image. Listed here so Dependabot
# still watches them.
sentence-transformers>=6.0.1
faiss-cpu>=1.9.0
